---
title: "L2test"
output: pdf_document
date: "2022-09-21"
---


pi * 3

10 %% 3

sign(-2)
sign(1)

a <- 4.4
b <- as.integer(a)
mode(a)
mode(b)

c <- 4.4
typeof(c)

typeof(as.integer(c)) 
mode(c) 


d <- 4.5
d <- as.complex(d)
 
mode(d) 
typeof(d)



c <- 4.5
mode(c)
typeof(c)

sqrt(2)

vec <- c(1, 2, 5)
vec ^ 2

vec[1]

c(1:3,4:6)

vec_pattern2 <- rep(1:3, times = 4, each = 2)

vec_seq2 <- seq(1, 10, by = 1)

data <- c(1, 2, 2, 3, 1, 2, 3, 3, 1, 2, 3, 3, 1)

data <- c(1, 2, 2, 3, 1, 2, 3, 3, 1, 2, 3, 3, 1)
fdata <- factor(data)
fdata

four_character <- c('d','c','a','b')
four_factor <- factor(four_character)

nlevels(four_factor)

fun <- c("programming", "in", "R")
fun2 <- c("Have", "fun")
fun3 <- c(fun2, fun)
fun3

x <- c(1, "SUSTech", TRUE)
x

temperatures <- c("warm", "cold", "hot", "hot", "hot", "cold")
three <- factor(temperatures,levels = c("hot","warm","cold"),ordered = FALSE)
sort(three)

dim1 <- c("A1", "A2", "A3", "A4")
dim2 <- c("B1", "B2", "B3")
dim3 <- c("C1", "C2")
(arr <- array(1:24, c(4, 3, 2), dimnames = list(dim1, dim2, dim3)))



